# Project TODOs

- [x] **`src/data/dataset.py`**: Implement image fetching and processing from URLs in the `preprocess_function`.
- [x] **`src/data/dataset.py`**: Choose and implement a text tokenizer for captions in the `preprocess_function`.
- [ ] **`src/forde/model.py`**: Define the overall model architecture (e.g., Vision Transformer with FORDE blocks).
- [ ] **`src/training/train.py`**: Implement a parallel `DataLoader` to accelerate data loading and hide I/O latency.